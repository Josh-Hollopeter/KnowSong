import { MyJsonpInterceptor } from './my-jsonp-interceptor';

describe('MyJsonpInterceptor', () => {
  it('should create an instance', () => {
    expect(new MyJsonpInterceptor()).toBeTruthy();
  });
});
